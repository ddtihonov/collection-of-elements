import React, {FC} from 'react'
import styles from './Headings.module.scss'

export const Headings: FC = () => {
    return (
        <section className={styles.main}>
            <h2 className={styles.title}>Headings</h2>
        </section>
    )
};