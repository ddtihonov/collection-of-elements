import React, {FC} from 'react'
import styles from './Preloaders.module.scss'

export const Preloaders: FC = () => {
    return (
        <section className={styles.main}>
            <h2 className={styles.title}>Preloaders</h2>
        </section>
    )
};